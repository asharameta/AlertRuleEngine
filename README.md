# AlertRuleEngine

## Build and Run

Build the project:

```bash
./gradlew build
```

Run the JAR:

```bash
java -jar build/libs/AlertRuleEngine.jar
```

On Windows, use `gradlew.bat` instead of `./gradlew`.